#ifndef MENUUPDATE_HPP
#define MENUUPDATE_HPP

unsigned int UpdateList(TextRenderer &textRenderer, const unsigned int menuPos,
                        const std::vector<std::string> &textVec);

#endif // MENUUPDATE_HPP

